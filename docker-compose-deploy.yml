version: "3.7"

services:
  app:
    build: .
    volumes:
      - static_data:/vol/web
    environment:
      - SECRET_KEY=15de58ft45efk91yp
      - ALLOWED_HOSTS=127.0.0.0,localhost,pmdsdata12,10.4.1.234
    restart: always

  proxy:
    build: ./proxy
    volumes:
      - static_data:/vol/static
    ports:
      - "80:80"
    depends_on:
      - app
    restart: always

volumes:
  static_data:
